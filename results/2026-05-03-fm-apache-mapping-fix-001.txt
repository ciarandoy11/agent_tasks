The previous attempt failed due to two critical issues: a **permissions error** during log writing and a persistent **404 Not Found** error, despite the Apache configuration being applied.

### 🔍 Root Cause Analysis
1.  **Log Failure:** The Worker attempted to create a `results/` directory in the local execution path, which resulted in `Permission denied`. All diagnostic data was lost because the script could not write to the file.
2.  **Persistence of 404 (The SELinux Wall):** The Apache mapping was likely implemented, but the requests still returned 404. On Fedora/Ultramarine, **SELinux** is enabled by default. Apache is strictly forbidden from accessing files on external mounts (like `/mnt/ext_ssd`) unless the files are explicitly labeled with the `httpd_sys_content_t` security context. Without this, Apache cannot "see" the directory, resulting in a 404.

### 🛠️ Engineering Plan
I will now instruct the Worker to:
1.  **Correct the Log Path:** Use `/mnt/ext_ssd/results/` to ensure write permissions.
2.  **Fix SELinux Contexts:** Apply the correct security labels to the Finance Manager directory so the `httpd` process can access the SSD.
3.  **Verify & Re-map:** Ensure the `/etc/httpd/conf.d/finance_manager.conf` is correct and restart the service.
4.  **Final Validation:** Perform the `curl` tests again to confirm the transition from 404 $\rightarrow$ 200 (or 500, which would at least prove the mapping is working).

MISSION BRIEF:
```json
{
  "target_tool_selection": "execute_bash",
  "worker_system_prompt": "Execute the provided bash script to fix the SELinux access issues and finalize the Apache mapping for Finance Manager. You MUST use the absolute path /mnt/ext_ssd/results/ for the output file to avoid permission errors. Ensure the SELinux context is updated and Apache is restarted.",
  "data_payload": "#!/bin/bash\n\n# Define the output file on the SSD to avoid permission issues\nOUTPUT_FILE=\"/mnt/ext_ssd/results/2026-05-03-fm-selinux-fix-001.txt\"\nCONFIG_FILE=\"/etc/httpd/conf.d/finance_manager.conf\"\nAPP_PATH=\"/mnt/ext_ssd/finance_manager\"\n\n# Ensure the results directory exists on SSD\nmkdir -p \"$(dirname \"$OUTPUT_FILE\")\"\n\necho \"--- Finance Manager - SELinux & Mapping Final Fix ---\" > \"$OUTPUT_FILE\"\necho \"Goal: Resolve persistent 404 by fixing SELinux contexts on the SSD mount.\" >> \"$OUTPUT_FILE\"\necho \"\" >> \"$OUTPUT_FILE\"\n\n# Step 1: Fix SELinux Contexts\necho \"--- 1. Fixing SELinux Contexts ---\" >> \"$OUTPUT_FILE\"\necho \"sudo chcon -R -t httpd_sys_content_t ${APP_PATH}\" >> \"$OUTPUT_FILE\"\nsudo chcon -R -t httpd_sys_content_t \"${APP_PATH}\" >> \"$OUTPUT_FILE\" 2>&1\necho \"Updating persistent SELinux policy...\" >> \"$OUTPUT_FILE\"\nsudo semanage fcontext -a -t httpd_sys_content_t \"${APP_PATH}(/.+)?\" 2>/dev/null >> \"$OUTPUT_FILE\" 2>&1\nsudo restorecon -R -v \"${APP_PATH}\" >> \"$OUTPUT_FILE\" 2>&1\necho \"\" >> \"$OUTPUT_FILE\"\n\n# Step 2: Ensure Apache Mapping is Correct\necho \"--- 2. Verifying Apache Mapping ---\" >> \"$OUTPUT_FILE\"\nsudo tee \"${CONFIG_FILE}\" > /dev/null << 'EOF_APACHE_CONF'\nAlias /finance_manager \"/mnt/ext_ssd/finance_manager\"\n<Directory \"/mnt/ext_ssd/finance_manager\">\n    Options Indexes FollowSymLinks\n    AllowOverride All\n    Require all granted\n    DirectoryIndex index.php finance_manager.php login.php\n</Directory>\nEOF_APACHE_CONF\necho \"Config written to ${CONFIG_FILE}.\" >> \"$OUTPUT_FILE\"\n\n# Step 3: Permissions check\necho \"--- 3. Final Permissions Check ---\" >> \"$OUTPUT_FILE\"\nsudo chown -R apache:apache \"${APP_PATH}\" >> \"$OUTPUT_FILE\" 2>&1\nsudo chmod -R 755 \"${APP_PATH}\" >> \"$OUTPUT_FILE\" 2>&1\n\n# Step 4: Restart and Validate\necho \"--- 4. Restarting Apache and Testing ---\" >> \"$OUTPUT_FILE\"\nsudo httpd -t >> \"$OUTPUT_FILE\" 2>&1\nsudo systemctl restart httpd >> \"$OUTPUT_FILE\" 2>&1\nsleep 2\n\n# Test Endpoints\nROOT_CODE=$(curl -o /dev/null -w \"%{http_code}\" -s http://localhost/finance_manager/)\necho \"/finance_manager/ HTTP Code: $ROOT_CODE\" >> \"$OUTPUT_FILE\"\n\nFM_CODE=$(curl -o /dev/null -w \"%{http_code}\" -s http://localhost/finance_manager/finance_manager.php)\necho \"finance_manager.php HTTP Code: $FM_CODE\" >> \"$OUTPUT_FILE\"\n\nLOGIN_CODE=$(curl -o /dev/null -w \"%{http_code}\" -s http://localhost/finance_manager/login.php)\necho \"login.php HTTP Code: $LOGIN_CODE\" >> \"$OUTPUT_FILE\"\n\n# Step 5: Log capture\necho \"\" >> \"$OUTPUT_FILE\"\necho \"--- 5. Final Error Log Excerpt ---\" >> \"$OUTPUT_FILE\"\nsudo tail -50 /var/log/httpd/error_log >> \"$OUTPUT_FILE\" 2>&1\n\n# Summary\necho \"\" >> \"$OUTPUT_FILE\"\necho \"--- Summary ---\"\nif [[ \"$FM_CODE\" == \"200\" ]]; then\n    echo \"SUCCESS: Finance Manager is now reachable (HTTP 200).\" >> \"$OUTPUT_FILE\"\nelif [[ \"$FM_CODE\" == \"500\" ]]; then\n    echo \"PARTIAL SUCCESS: Mapping works, but app has internal error (HTTP 500).\" >> \"$OUTPUT_FILE\"\nelse\n    echo \"FAILED: Still receiving HTTP $FM_CODE. Check logs for further blockers.\" >> \"$OUTPUT_FILE\"\nfi\n\necho \"Mission brief executed. Full results written to $OUTPUT_FILE\""
}
```